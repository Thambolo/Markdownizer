# ============================================================================
# Markdownizer Agent Configuration
# Copy this file to .env and fill in your values
# ============================================================================

# ============================================================================
# Model Configuration (Auto-Fallback System)
# ============================================================================
# By default, Markdownizer uses ConnectOnion's FREE tier (100k tokens/month)
# If that fails, it falls back to your OpenAI API key
# This means you can try the agent WITHOUT providing any API key!

# USE_CONNECTONION_FREE=true  # Try ConnectOnion free tier first (default: true)
# CONNECTONION_MODEL=co/gpt-4o-mini  # Free model name (default: co/gpt-4o-mini)

# ============================================================================
# OPTIONAL: OpenAI API Configuration (Fallback)
# ============================================================================
# Only needed if:
# - ConnectOnion free tier is unavailable, or
# - You want to use a specific OpenAI model, or
# - You exceed ConnectOnion's free tier limit (100k tokens/month)
#
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Change the OpenAI fallback model (default: gpt-4o-mini)
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
# OPENAI_MODEL=gpt-4o-mini

# ============================================================================
# ConnectOnion Configuration (Optional - for platform features)
# ============================================================================
# Only needed if using ConnectOnion CLI tools (co auth, co init) or platform features
# OPENONION_API_KEY=your-openonion-api-key-here
# CONNECTONION_API_KEY=your-connectonion-api-key-here
# AGENT_EMAIL=your-agent@mail.openonion.ai

# ============================================================================
# Server Configuration
# ============================================================================
# Host to bind the server to (default: 127.0.0.1)
# SERVER_HOST=127.0.0.1

# Port to run the server on (default: 5050)
# SERVER_PORT=5050

# Enable auto-reload on code changes (default: true)
# SERVER_RELOAD=true

# ============================================================================
# Playwright Configuration (Optional - for blocker detection)
# ============================================================================
# Run Playwright in headless mode (default: true)
# PLAYWRIGHT_HEADLESS=true

# Playwright timeout in seconds (default: 30)
# PLAYWRIGHT_TIMEOUT=30

# ============================================================================
# HTTP Client Configuration
# ============================================================================
# HTTP request timeout in seconds (default: 15)
# HTTP_TIMEOUT=15

# Maximum number of retries for failed requests (default: 3)
# HTTP_MAX_RETRIES=3

# User agent string for HTTP requests (default: Chrome 120)
# HTTP_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36

# ============================================================================
# Comparison & Scoring Configuration
# ============================================================================
# Score difference threshold for choosing between sources (default: 0.05)
# Lower = more aggressive switching, Higher = prefer Readability more often
# SCORE_THRESHOLD=0.05

# Minimum content length in characters (default: 100)
# MIN_CONTENT_LENGTH=100

# Maximum content length for normalization in characters (default: 50000)
# MAX_CONTENT_LENGTH=50000
